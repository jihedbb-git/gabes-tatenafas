# Optional Python sidecar for the academic "pro" version of:
#   - train_augment.py   (TimeGAN-style data augmentation)
#   - train_forecast.py  (real XGBoost + LSTM ensemble)
#   - predict.py         (writes forecast_predictions table)
#
# Install (once):
#   pip install -r scripts/requirements.txt
#
# The PHP project works WITHOUT these — they only unlock the deep-learning
# experiments referenced in your thesis / mémoire.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
tensorflow>=2.13       # provides keras for LSTM
matplotlib>=3.7
pymysql>=1.1
python-dotenv>=1.0
